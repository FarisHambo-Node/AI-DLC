"""Jira adapter — tickets + transitions + comments."""

# TODO: class JiraAdapter
#   - create_ticket(title, description, issue_type, labels) -> ticket_ref
#   - transition(ticket_ref, status)
#   - add_comment(ticket_ref, body)
